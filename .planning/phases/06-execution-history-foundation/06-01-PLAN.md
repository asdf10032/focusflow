# 06-01 Plan: Execution History Data Foundation

## Goal

Add the durable execution history storage needed by v1.1 without changing the existing frontend flow.

## Scope

- Add an `execution_logs` SQLAlchemy model.
- Add Alembic migration `0002_execution_logs.py`.
- Register the model so test metadata creation includes the table.
- Add execution schemas for feedback requests and history responses.
- Snapshot `task_title_snapshot` and `estimated_minutes_snapshot` at feedback time.

## Requirements

- HIST-01
- HIST-02
- EST-03
- NFR-01

## Verification

- History table exists in `Base.metadata.create_all()` tests.
- `python -m compileall backend alembic` passes.

