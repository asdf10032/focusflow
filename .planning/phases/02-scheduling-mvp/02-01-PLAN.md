---
phase: 02-scheduling-mvp
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/endpoints/schedules.py
  - tests/test_phase1_api.py
autonomous: true
requirements: [REQ-SCHEDULE-3PLANS, REQ-SCHEDULE-SELECT, NFR-TEST]
user_setup: []
must_haves:
  truths:
    - "Generate persists exactly three plans for a requested date."
    - "Persisted plans contain schedule items generated by the scheduler."
    - "Blocked slots are not used by generated plan items."
    - "Selecting a plan marks exactly one same-day plan as selected."
  artifacts:
    - path: "backend/app/api/v1/endpoints/schedules.py"
      provides: "Schedule generate/select persistence behavior"
    - path: "tests/test_phase1_api.py"
      provides: "Regression coverage for schedule persistence and selection"
  key_links:
    - from: "backend/app/api/v1/endpoints/schedules.py"
      to: "backend/app/models/schedule_plan.py"
      via: "SQLAlchemy persistence"
      pattern: "SchedulePlan"
---

<objective>
Persist generated schedule plans and implement selectable plan state.

Purpose: The frontend and execution flow need generated schedule plans to survive beyond the request that created them.
Output: Working persistence for `/schedules/generate` and `/schedules/select`.
</objective>

<execution_context>
@C:/Users/HUAWEI/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HUAWEI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/api/v1/endpoints/schedules.py
@backend/app/services/scheduler/engine.py
@backend/app/models/schedule_plan.py
@backend/app/models/schedule_item.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistence tests for schedule generation</name>
  <files>tests/test_phase1_api.py</files>
  <action>Extend API tests to assert generate persists three plans and avoids a blocked first slot.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Test fails before persistence implementation and passes after.</done>
</task>

<task type="auto">
  <name>Task 2: Persist generated plans and items</name>
  <files>backend/app/api/v1/endpoints/schedules.py</files>
  <action>Replace same-day plans on generate, create SchedulePlan rows, and append ScheduleItem rows for each generated segment.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Generate is idempotent for a date and stores three plans with items.</done>
</task>

<task type="auto">
  <name>Task 3: Implement schedule selection</name>
  <files>backend/app/api/v1/endpoints/schedules.py</files>
  <action>Find the requested same-day plan, set it selected, clear selected on sibling plans, and return selected metadata.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Only one plan is selected for the requested date.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider
- [ ] python -m compileall backend alembic
- [ ] npm.cmd run build
</verification>

<success_criteria>
- Generate persists three same-day plans.
- Generate replaces previous same-day generated plans rather than duplicating them.
- Select marks exactly one same-day plan as selected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scheduling-mvp/02-01-SUMMARY.md`
</output>
