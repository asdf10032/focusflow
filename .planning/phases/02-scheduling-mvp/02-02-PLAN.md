---
phase: 02-scheduling-mvp
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/services/scheduler/engine.py
  - backend/app/api/v1/endpoints/schedules.py
  - backend/app/schemas/schedule.py
  - tests/test_scheduler_engine.py
autonomous: true
requirements: [REQ-SCHEDULE-3PLANS, REQ-SCHEDULE-SELECT, NFR-TEST]
user_setup: []
must_haves:
  truths:
    - "Scheduler detects impossible or cyclic dependency inputs before placing tasks."
    - "Unplaced tasks are reported in a stable response field."
    - "Plan scores reflect useful placement quality rather than only item count."
    - "Risk or warning output is available for tasks that cannot be scheduled."
  artifacts:
    - path: "backend/app/services/scheduler/engine.py"
      provides: "Scheduler dependency, scoring, and warning logic"
    - path: "tests/test_scheduler_engine.py"
      provides: "Focused scheduler unit coverage"
  key_links:
    - from: "backend/app/api/v1/endpoints/schedules.py"
      to: "backend/app/services/scheduler/engine.py"
      via: "generate_plans"
      pattern: "generate_plans"
---

<objective>
Complete the remaining scheduling MVP correctness checks.

Purpose: Plan persistence works, but the scheduler still needs stronger dependency validation, scoring, and warning coverage before the frontend treats its output as reliable.
Output: Focused scheduler tests and engine/API updates for dependency, unplaced, and risk behavior.
</objective>

<execution_context>
@C:/Users/HUAWEI/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HUAWEI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scheduling-mvp/02-01-SUMMARY.md
@backend/app/services/scheduler/engine.py
@backend/app/api/v1/endpoints/schedules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduler unit tests</name>
  <files>tests/test_scheduler_engine.py</files>
  <action>Create focused tests for dependency ordering/cycle detection, full-day blocked unplaced output, and score/risk expectations.</action>
  <verify>python -m pytest tests/test_scheduler_engine.py -q -p no:cacheprovider</verify>
  <done>Tests fail first against the current minimal greedy implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Harden scheduler dependency and warning behavior</name>
  <files>backend/app/services/scheduler/engine.py, backend/app/schemas/schedule.py</files>
  <action>Add dependency validation before placement and include stable warning/risk information for unplaced tasks.</action>
  <verify>python -m pytest tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Dependency and warning behavior passes without regressing API tests.</done>
</task>

<task type="auto">
  <name>Task 3: Improve scoring and API exposure</name>
  <files>backend/app/services/scheduler/engine.py, backend/app/api/v1/endpoints/schedules.py</files>
  <action>Make scores reflect schedule quality and ensure generated API output exposes the fields needed by Phase 3 calendar UI.</action>
  <verify>python -m pytest tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Three plans remain stable and carry useful score/risk data.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider
- [ ] python -m compileall backend alembic
- [ ] npm.cmd run build
</verification>

<success_criteria>
- Scheduler has direct unit tests.
- Dependency and unplaced task behavior is deterministic.
- Existing Phase 1 and Phase 2 API tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scheduling-mvp/02-02-SUMMARY.md`
</output>
