---
phase: 03.1-bilingual-ui-toggle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/Tasks.tsx
  - frontend/src/pages/Calendar.tsx
  - frontend/src/state/store.ts
  - frontend/src/lib/i18n.ts
  - tests/test_frontend_i18n.py
autonomous: true
requirements: [NFR-UX]
user_setup: []
must_haves:
  truths:
    - "The web UI can switch between Chinese and English from the header."
    - "Language choice persists across page reloads through localStorage."
    - "Task and calendar visible UI strings use the same translation mechanism."
    - "Backend API payload field names and plan_type values remain unchanged."
  artifacts:
    - path: "frontend/src/lib/i18n.ts"
      provides: "Translation dictionary, language types, lookup helper, and persistence keys"
    - path: "frontend/src/state/store.ts"
      provides: "Current language state and setter"
    - path: "frontend/src/App.tsx"
      provides: "Header language toggle"
    - path: "frontend/src/pages/Tasks.tsx"
      provides: "Localized task management UI"
    - path: "frontend/src/pages/Calendar.tsx"
      provides: "Localized calendar and schedule selection UI"
    - path: "tests/test_frontend_i18n.py"
      provides: "Regression coverage for translation completeness and persistence helper behavior"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/state/store.ts"
      via: "language state"
      pattern: "setLanguage"
    - from: "frontend/src/pages/Tasks.tsx"
      to: "frontend/src/lib/i18n.ts"
      via: "translation lookup"
      pattern: "t(language,"
    - from: "frontend/src/pages/Calendar.tsx"
      to: "frontend/src/lib/i18n.ts"
      via: "translation lookup"
      pattern: "t(language,"
---

<objective>
Add a Chinese/English UI language switch to the frontend.

Purpose: Phase 4 will add more user-facing screens; adding a lightweight bilingual mechanism now prevents scattered hard-coded strings from spreading.
Output: A header language toggle with persistent `zh-CN` / `en` selection and localized visible strings for the current Phase 3 task/calendar flow.
</objective>

<execution_context>
@C:/Users/HUAWEI/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HUAWEI/.codex/get-shit-done/templates/summary.md
@C:/Users/HUAWEI/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-main-flow/03-VERIFICATION.md
@.planning/phases/03.1-bilingual-ui-toggle/03.1-VALIDATION.md
@frontend/src/App.tsx
@frontend/src/pages/Tasks.tsx
@frontend/src/pages/Calendar.tsx
@frontend/src/state/store.ts
@frontend/src/lib/scheduleView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n tests first</name>
  <files>tests/test_frontend_i18n.py</files>
  <action>Add tests that assert every translation key exists for `en` and `zh-CN`, status labels are localized, and the language storage key/defaults are stable.</action>
  <verify>python -m pytest tests/test_frontend_i18n.py -q -p no:cacheprovider</verify>
  <done>Tests fail before `frontend/src/lib/i18n.ts` exists and pass after the helper is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lightweight i18n state and helpers</name>
  <files>frontend/src/lib/i18n.ts, frontend/src/state/store.ts</files>
  <action>Create language types, translation dictionaries, `t()` lookup helper, status/plan labels, localStorage load/save helpers, and Zustand language state.</action>
  <verify>python -m pytest tests/test_frontend_i18n.py -q -p no:cacheprovider</verify>
  <done>Translation completeness and persistence-helper tests pass without introducing a third-party i18n dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Add header language toggle</name>
  <files>frontend/src/App.tsx</files>
  <action>Add a compact Chinese/English toggle in the header, localize nav labels, and persist changes via the store/localStorage helper.</action>
  <verify>npm.cmd run build</verify>
  <done>Header exposes language switching and build remains green.</done>
</task>

<task type="auto">
  <name>Task 4: Localize task and calendar pages</name>
  <files>frontend/src/pages/Tasks.tsx, frontend/src/pages/Calendar.tsx</files>
  <action>Replace visible hard-coded UI strings with translation lookups for task forms, task list, schedule generation, variant selection, empty states, warnings, and action messages.</action>
  <verify>npm.cmd run build</verify>
  <done>Task and calendar flows remain functional in both languages.</done>
</task>

<task type="auto">
  <name>Task 5: Run phase verification</name>
  <files>.planning/phases/03.1-bilingual-ui-toggle/03.1-01-SUMMARY.md, .planning/phases/03.1-bilingual-ui-toggle/03.1-VERIFICATION.md</files>
  <action>Run frontend i18n tests, existing backend/frontend regression tests, TypeScript/Vite build, and a local browser visibility check for both languages when practical.</action>
  <verify>python -m pytest tests/test_frontend_i18n.py tests/test_frontend_view_helpers.py tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider; npm.cmd run build</verify>
  <done>Summary and verification artifacts record the passing evidence.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_frontend_i18n.py -q -p no:cacheprovider
- [ ] python -m pytest tests/test_frontend_i18n.py tests/test_frontend_view_helpers.py tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider
- [ ] python -m compileall backend alembic
- [ ] npm.cmd run build
- [ ] Local browser visibility check confirms Chinese and English labels render without a blank page
</verification>

<success_criteria>
- Header includes a clear Chinese/English language control.
- Language selection persists across reloads.
- Current task and calendar UI can render in Chinese and English.
- Backend contracts remain untouched.
- Existing Phase 1-3 tests and frontend build still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-bilingual-ui-toggle/03.1-01-SUMMARY.md` and `.planning/phases/03.1-bilingual-ui-toggle/03.1-VERIFICATION.md`.
</output>
