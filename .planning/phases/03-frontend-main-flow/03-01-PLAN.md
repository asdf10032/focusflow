---
phase: 03-frontend-main-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/pages/Tasks.tsx
  - frontend/src/pages/Calendar.tsx
  - frontend/src/state/store.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/scheduleView.ts
  - tests/test_frontend_view_helpers.py
autonomous: true
requirements: [REQ-CALENDAR-VIEW, REQ-TASK-CRUD, REQ-SCHEDULE-SELECT]
user_setup: []
must_haves:
  truths:
    - "Task list supports create, edit, delete, and status visibility."
    - "Calendar page renders generated schedule plan items."
    - "User can switch among conservative, balanced, and aggressive variants."
    - "User can select a generated plan from the frontend."
  artifacts:
    - path: "frontend/src/pages/Tasks.tsx"
      provides: "Task create/edit/delete/status UI"
    - path: "frontend/src/pages/Calendar.tsx"
      provides: "Generate, switch, render, and select schedule plans"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed API client for unified response envelopes"
    - path: "tests/test_frontend_view_helpers.py"
      provides: "Regression coverage for schedule view helper behavior"
  key_links:
    - from: "frontend/src/pages/Calendar.tsx"
      to: "backend/app/api/v1/endpoints/schedules.py"
      via: "POST /api/v1/schedules/generate and /api/v1/schedules/select"
      pattern: "generateSchedule"
    - from: "frontend/src/pages/Tasks.tsx"
      to: "backend/app/api/v1/endpoints/tasks.py"
      via: "Task CRUD endpoints"
      pattern: "createTask"
---

<objective>
Implement the frontend MVP loop for task management and daily plan selection.

Purpose: Phase 2 made schedule generation reliable; Phase 3 makes that capability usable from the web UI.
Output: A working React interface where users can manage tasks, generate today's three plans, switch variants, inspect items, and select one plan.
</objective>

<execution_context>
@C:/Users/HUAWEI/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HUAWEI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scheduling-mvp/02-VERIFICATION.md
@frontend/src/App.tsx
@frontend/src/pages/Tasks.tsx
@frontend/src/pages/Calendar.tsx
@frontend/src/state/store.ts
@backend/app/api/v1/endpoints/tasks.py
@backend/app/api/v1/endpoints/schedules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend helper tests</name>
  <files>tests/test_frontend_view_helpers.py</files>
  <action>Add tests for plan-type labeling, schedule-item sorting, and time-range formatting used by the calendar UI.</action>
  <verify>python -m pytest tests/test_frontend_view_helpers.py -q -p no:cacheprovider</verify>
  <done>Tests fail before the helper module exists and pass after helper implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed API and store capabilities</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/scheduleView.ts, frontend/src/state/store.ts</files>
  <action>Create a small typed API client for the backend envelope, define task/schedule types, and store generated plans plus selected variant state.</action>
  <verify>python -m pytest tests/test_frontend_view_helpers.py -q -p no:cacheprovider</verify>
  <done>Helper tests pass and TypeScript can consume API/store types.</done>
</task>

<task type="auto">
  <name>Task 3: Implement task management UI</name>
  <files>frontend/src/pages/Tasks.tsx</files>
  <action>Replace the read-only task placeholder with create, edit, delete, refresh, and status controls using the task API.</action>
  <verify>npm.cmd run build</verify>
  <done>Task page builds and supports the required CRUD surface.</done>
</task>

<task type="auto">
  <name>Task 4: Implement calendar plan UI</name>
  <files>frontend/src/pages/Calendar.tsx, frontend/src/App.tsx, frontend/src/index.css</files>
  <action>Build the daily schedule page with date input, generate button, plan variant tabs, selected-plan action, warnings, and a readable time-grid/list view.</action>
  <verify>npm.cmd run build</verify>
  <done>Calendar page renders generated plan items and can submit the selected variant.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_frontend_view_helpers.py tests/test_scheduler_engine.py tests/test_phase1_api.py -q -p no:cacheprovider
- [ ] python -m compileall backend alembic
- [ ] npm.cmd run build
</verification>

<success_criteria>
- Task list supports create, edit, delete, and status visibility.
- Calendar page renders generated plan items.
- User can switch among conservative, balanced, and aggressive variants.
- User can select a plan from the frontend.
- Existing backend API and scheduler tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-main-flow/03-01-SUMMARY.md` and `.planning/phases/03-frontend-main-flow/03-VERIFICATION.md`.
</output>
