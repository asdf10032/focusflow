---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/endpoints/projects.py
  - backend/app/api/v1/endpoints/tasks.py
  - backend/app/api/v1/endpoints/energy.py
  - backend/app/schemas/schedule.py
  - backend/app/schemas/project.py
  - backend/app/schemas/task.py
  - backend/app/schemas/energy.py
  - tests/test_phase1_api.py
autonomous: true
requirements: [REQ-PROJ-CRUD, REQ-TASK-CRUD, REQ-ENERGY, REQ-BLOCKED, REQ-DEFAULT-SEEDS, NFR-API, NFR-VALIDATION]
user_setup: []
must_haves:
  truths:
    - "Health endpoint returns a success envelope."
    - "Projects, tasks, energy templates, and blocked times return success envelopes."
    - "Default energy templates seed six templates with 48 slots each."
    - "Schedule generation accepts an ISO date and returns three plan types."
  artifacts:
    - path: "tests/test_phase1_api.py"
      provides: "Phase 1 API regression tests"
      min_lines: 80
    - path: "backend/app/api/v1/endpoints/projects.py"
      provides: "Project CRUD envelope responses"
    - path: "backend/app/api/v1/endpoints/tasks.py"
      provides: "Task CRUD envelope responses"
    - path: "backend/app/api/v1/endpoints/energy.py"
      provides: "Energy and blocked-time envelope responses"
  key_links:
    - from: "backend/app/api/v1/endpoints/projects.py"
      to: "backend/app/core/response.py"
      via: "success envelope helper"
      pattern: "success\\("
---

<objective>
Stabilize the Phase 1 foundation API contract with regression tests.

Purpose: Phase 2 scheduling work needs a trustworthy backend baseline and a consistent envelope shape.
Output: API tests plus endpoint/schema fixes for Phase 1.
</objective>

<execution_context>
@C:/Users/HUAWEI/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HUAWEI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/main.py
@backend/app/core/response.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 1 API regression tests</name>
  <files>tests/test_phase1_api.py</files>
  <action>Write tests for health, project/task CRUD, energy template seeds, blocked times, and schedule generation envelope behavior.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q</verify>
  <done>Tests fail first on current raw CRUD responses, then pass after fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Make CRUD endpoints use the success envelope</name>
  <files>backend/app/api/v1/endpoints/projects.py, backend/app/api/v1/endpoints/tasks.py, backend/app/api/v1/endpoints/energy.py</files>
  <action>Return success(data) consistently and serialize ORM models through Pydantic output schemas or explicit dictionaries.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Project, task, energy, and blocked-time responses all expose status/data/error/meta.</done>
</task>

<task type="auto">
  <name>Task 3: Clean schema compatibility</name>
  <files>backend/app/schemas/schedule.py, backend/app/schemas/project.py, backend/app/schemas/task.py, backend/app/schemas/energy.py</files>
  <action>Fix the schedule request date annotation and replace deprecated Pydantic Config usage with ConfigDict.</action>
  <verify>python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider</verify>
  <done>Tests pass without Pydantic deprecation warnings.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_phase1_api.py -q -p no:cacheprovider
- [ ] python -m compileall backend alembic
- [ ] npm.cmd run build
</verification>

<success_criteria>
- All Phase 1 API regression tests pass.
- Backend files compile successfully.
- Frontend production build succeeds.
- GSD Phase 1 has a matching SUMMARY artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
